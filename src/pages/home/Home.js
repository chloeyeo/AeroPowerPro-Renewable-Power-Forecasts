import React from "react";
import { NavBar } from "components";
import { SearchBar } from "./components";

const Home = () => (
  <div>
    <NavBar />
    <SearchBar />
  </div>
);
export default Home;
